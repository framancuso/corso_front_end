{
  "name": "roboplanning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "pug --watch index.pug",
    "watch1": "sass --watch style:style",
    "dev": "concurrently \"npm run watch\" \"npm run watch1\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
